# Tang（百事录·唐 / Damoxing）变更日志

> 记录本项目所有功能性改动，按时间正序排列（最早 → 最新）。
> 每一笔改动均遵循工作流：功能分支（`feat/…`、`fix/…`）→ 提交 → 合并回 `main` → 删除分支。
> 项目根：`D:\DEEPSEEK\应用\8.29UI\Tang-main`　引擎：Godot 4.7.2 stable（Mobile 渲染器）

---

## 2026-08-30

### [chore] 8.29UI 水墨版基线 — `504f9a8`
- 新增 ink 水墨素材（`res://assets/ui/ink/`），UI 全面重构为水墨风格。
- 建立「图鉴 / 大事记 / 群聊 / 知识卡片」面板体系与水墨绘制管线（`scenes/ui_overlay.gd`）。

### [fix] 优化 UI hover 与时间轴交互 — `c2ed00e`
- 统一 hover 检测：`_detect_ui_hover()` 返回带语义的 key，绘制层据此渲染悬停/按下态。
- 时间轴事件点可悬停高亮、点击响应。

### [fix] 修复时间轴事件点 hover 检测的类型推断错误 — `7e61c14`
- `var ev_idx := timeline_event_at_x(p.x)` 显式标注 `: int`，消除 GDScript 推断错误。

### [feat] 左侧栏收起展开 + UI 交互修复 — `d69551e`
- 左侧功能栏支持收起/展开，收起后仅保留窄条 + 展开按钮。
- 相关 UI 交互细节修复。

### [fix] 修复左侧栏收起按钮与时钟指针中心 — `4451815`
- 收起按钮命中区域与时钟指针圆心对齐修正。

### [fix] 修复 Overlay 初始化时 map 为空导致 left_toggle_rect 调用崩溃 — `2564cb1`
- `_ready()` 阶段 `map` 尚未就绪时对 `left_toggle_rect()` 的调用加空值保护。

### [feat] 清理左侧按钮贴图 + 时间轴收起展开 + 年份滑动动画 — `e0e797e`
- 左侧按钮统一为「知识卡片选项」视觉，覆盖圆形按钮贴图。
- 时间轴支持上下收起/展开（底部窄条 + 展开按钮）。
- 时间轴大圆年份平滑滑动动画（`_year_display` 插值）。

### [fix] 左侧收起背景 + 时间轴上下收起 + 知识卡片避让时钟 — `51d03ca`
- 收起状态下左侧墨雾背景彻底收起；知识卡片位置避让时钟区域。

### [fix] 左侧收起背景彻底收起 + 知识卡片下移 + 时间轴收起按钮居中 — `e1c92c3`
- 背景、卡片位置、收起按钮居中等细节收敛。

### [fix] 选中地块菱形描边移至 UI 下方 — `1916a7a`
- 坊市选中/悬停的菱形描边（`scenes/outline_layer.gd`）绘制层级调整到 UI 之下。

### [fix] 大事记跳转时时钟与大圆同步运动 — `c3898fc`
- 点击时间轴事件 → 年份跳转动画：时钟（太阳月亮）流转的同时，时间轴大圆同步滑向目标年份。

### [fix] 图鉴居中 + 关闭按钮 + 滚动防溢出 — `75b86bf`
- 图鉴面板居中显示；右上角新增关闭按钮；滚动位置防溢出。

### [fix] 图鉴/大事记上移 + 列表遮罩式裁剪 — `f56e862`
- 面板位置上移；列表区采用 `clip_contents` 子容器（`scenes/clip_list.gd`）实现遮罩式滚动裁剪。

### [fix] 修复裁剪容器绘制上下文，条目重新可见 — `8ffb563`
- 裁剪容器条目改为以容器自身为绘制上下文（而非 Overlay 闭包），条目恢复可见。

### [fix] 关闭图鉴/大事记后裁剪容器条目残留 — `1fa5ca6`
- 面板关闭时同步隐藏对应裁剪容器，避免残留内容。

### [feat] 主菜单按钮点击提示（功能正在开发中）— `6691bf7`
- 主菜单各按钮点击弹出「功能正在开发中」提示。

### [feat] 主菜单左右切换关卡按钮 — `c0700bc`
- 主菜单新增左右箭头，切换关卡。

### [feat] 主菜单关卡标签对齐与新增 — `cf13173`
- 关卡标签对齐修正与新增。

### [feat] 佛塔缩小一半高 + 长安标签移到宫殿右侧 — `8396959`
- 主菜单背景佛塔缩小，长安标签位置调整。

### [fix] 佛塔改为等比缩放 + 长安标签左移 — `0b3e78a`
- 佛塔等比缩放（避免变形），标签再微调。

### [feat] 新增 Start 标题界面（百事录·唐）— `4c2c548`
- 新建 `scenes/Start.tscn` + `scenes/start.gd`：标题界面、背景视差、建筑群、进入按钮。
- `project.godot` 主场景改为 `res://scenes/Start.tscn`。

### [feat] 主菜单左下角新增返回按钮 — `095384d`
- 主菜单左下角「返回」按钮 → 回到 Start 标题界面。

### [fix] 设置卡片分辨率按钮超出边框 — `8dceeaa`
- 设置卡片内分辨率选项按钮边界修正。

### [feat] 场景切换渐暗黑屏转场 — `e990813`
- 新增 autoload `SceneTransition.gd`：fade 0.25s → 黑屏 0.25s → 切场景 → fade in 0.3s。
- 全屏黑色 ColorRect（layer 200）覆盖转场。

### [feat] 背景视差滚动 + 卡片标题居中 + start 建筑群 — `e6deb4a`
- Start / 主菜单背景多层视差滚动。
- 卡片标题居中；Start 界面加入建筑群。

### [feat] 白云萦绕山腰 + 建筑错落拉开 + mainmenu 性能优化 + 行走工人 — `173c9e0`
- 白云萦绕山腰；建筑错落分布；主菜单预渲染性能优化；加入行走工人（小人）。

### [fix] 移除小人 + 白云图层修复 + 建筑均匀分布/淡入淡出 + 性能优化 — `7beac68`
- 移除行走工人（小人视觉效果不佳）；修复白云绘制层级（被山体遮挡的问题）。
- 建筑均匀分布、淡入淡出修正；性能再优化。

### [fix] 删除白云 + 建筑三层分层 + 淡出修正 — `99547fb`
- 按用户要求彻底删除白云。
- 建筑分三层（远/中/近），逐层淡出修正。

### [fix] 修正建筑三层遮挡关系（前遮中、中遮后）— `765bda2`
- 建筑按 `base_y` 升序排序：前层建筑正确遮挡中层、中层遮挡后层。

---

## 2026-08-31

### [feat] 删除右上角钟表，改为地平线+太阳月亮时间指示 — `9d5cf80`
- 移除右上角钟表（`CLOCK_RECT` 及 `_draw_clock_hands`）。
- 改为「地平线 + 太阳/月亮弧线」时间指示：太阳 6–18 时可见（6 时左地平线 → 12 时正上方 → 18 时右地平线），月亮为太阳 +12h 相位（夜间可见，午夜在正上方）。

### [feat] 时间指示背景变色 + 太阳月亮放大 + 时辰切换 + 时间轴动画恢复 — `c9b42d9`
- 时间指示背景随时辰平滑变色（午夜黑 → 晨红 → 正午白 → 傍晚红 → 午夜黑，`t*t` 缓动）。
- 太阳/月亮放大（光晕、核心、光芒尺寸增大），夜晚加星星（固定 seed 2024）。
- 点击时间区域循环切换时辰（每 2 小时一跳）。
- 时间轴事件点击恢复年份跳转动画（太阳月亮流转 + 大圆滑动）。

### [fix] 恢复时辰选择弹窗卡片 + 时间指示背景地平线以下固定黑色、文字固定白色 — `3898b67`
- **时辰选择弹窗卡片恢复**：点击时间指示区域弹出 12 时辰卡片（水墨面板 + 时辰项，当前时辰高亮、hover 效果）；点时辰项切换时间并关闭，点弹窗外关闭。
- **背景修复**：时间指示面板地平线以上随时辰变色，地平线以下**固定黑色**（`Color(0.04,0.04,0.05,0.98)`），时辰文字**固定纯白** `Color(1,1,1,1)`，不再受背景色影响。

### [fix] 时辰切换始终顺时针推进并精确落位 + 时间轴跳转按过去/未来决定日月转向 — `8e95083`
- **时辰错位修复**：旧实现 lerp 停在 `|diff| ≤ 0.02`，目标 14（未时）实际停在 13.98 → 显示午时（早一个时辰）。改为 `_time_anim` 动画结束时 `fposmod(_time_anim_to, 24.0)` **精确落位**。
- **不再时间倒流**：点击更早的时辰时，目标不早于当前时刻（必要时 `+24h` 绕一圈），太阳月亮始终顺时针正向转。
- **时间轴方向**：点击更早年份 → 太阳月亮**逆时针**转（`_year_dir = -1`）；更晚年份 → **顺时针**转（`_year_dir = +1`）。手动调时会打断进行中的年份跳转动画。
- 点击当前时辰直接到位，不空转。

### [fix] 点击时间轴恢复打开大事记卡片（事件点点击不再被年份跳转吞掉）— `98948ac`
- 修复「时间轴动画恢复」引入的回归：点击时间轴**事件点**时 `_jump_to_year()` 会执行 `_hist_open = false`，导致大事记卡片不出现。
- 现在点击时间轴（事件点或空白处）都先打开大事记卡片；事件点点击叠加滚动定位 + 年份跳转动画。
- `_jump_to_year` 不再擅自关闭卡片，关闭逻辑由调用处控制（卡片内点事件关卡片 → 跳转的行为不变）。

---

## 当前状态

- `main` 分支干净，工作区无未提交改动。
- 编辑器以 `-e --path … -- --mcp-server` 运行，MCP 服务在 `http://127.0.0.1:9080/mcp`。
- 主场景：`res://scenes/Start.tscn`（Start 标题 → 主菜单 → 长安城地图）。
